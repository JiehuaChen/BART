We keep this empty directory tracked in the git repository because bartcode_v2_101512.R requires setting this directory as a working directory using setwd().
The MCMC results are then written to this directory. However, we do not need to track those results in the git repository.
- W.Wu, 2013 January 7
